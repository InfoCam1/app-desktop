<Window x:Class="InfoCam.Views.CameraGalleryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Galería de Cámaras" Height="600" Width="800"
        Icon="pack://application:,,,/app_icon.png"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Galería de Imágenes de Cámaras" FontSize="20" FontWeight="Bold" 
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1">
            <ItemsControl Name="CamerasItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SurfaceBrush}" Margin="10" Padding="10" CornerRadius="5" Width="220">
                            <StackPanel>
                                <Image Source="{Binding Imagen}" Height="150" Stretch="Uniform" Margin="0,0,0,10">
                                    <Image.ToolTip>
                                        <TextBlock Text="{Binding Nombre}"/>
                                    </Image.ToolTip>
                                </Image>
                                <TextBlock Text="{Binding Nombre}" Foreground="{StaticResource TextPrimaryBrush}" 
                                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Id, StringFormat='ID: {0}'}" Foreground="{StaticResource TextSecondaryBrush}" 
                                           HorizontalAlignment="Center" FontSize="11" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Cerrar" Click="CloseButton_Click" Width="100" Height="35" 
                HorizontalAlignment="Right" Margin="0,15,0,0"
                Style="{StaticResource ModernButtonStyle}"/>
    </Grid>
</Window>
