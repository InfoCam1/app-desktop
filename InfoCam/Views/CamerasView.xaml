<UserControl x:Class="InfoCam.Views.CamerasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Visualizar Imágenes" Click="ViewGallery_Click" Style="{StaticResource ModernButtonStyle}" Background="{StaticResource AccentBrush}" Height="29" Width="150" Margin="0,0,10,0"/>
            <Button Content="Eliminar Seleccionadas" Click="DeleteButton_Click" Style="{StaticResource ModernButtonStyle}" Background="#FF5252" Height="29" Width="150" Margin="0,0,10,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Name="CamerasGrid" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="Horizontal" Margin="10" IsReadOnly="True" SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="250"/>
                <DataGridTextColumn Header="Latitud" Binding="{Binding Latitud}" Width="100"/>
                <DataGridTextColumn Header="Longitud" Binding="{Binding Longitud}" Width="100"/>
                <DataGridCheckBoxColumn Header="Activa" Binding="{Binding Activa}" Width="70"/>
                <DataGridTextColumn Header="Imagen URL" Binding="{Binding Imagen}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>